@model IEnumerable<odevweb.Models.Personel>

@{
    ViewData["Title"] = "PersonelIndex";
}

<!---
<h1>PersonelIndex</h1>

<p>
    <a asp-action="PersonelCreate">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Html.DisplayNameFor(model => model.PersonelAd)
            </th>
            <th>
               Html.DisplayNameFor(model => model.PersonelSoyad)
            </th>
            <th>
               Html.DisplayNameFor(model => model.Islem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
foreach (var item in Model) {
        <tr>
            <td>
                Html.DisplayFor(modelItem => item.PersonelAd)
            </td>
            <td>
                Html.DisplayFor(modelItem => item.PersonelSoyad)
            </td>
            <td>
                Html.DisplayFor(modelItem => item.Islem.IslemAd)
            </td>
            <td>
                <a asp-action="PersonelEdit" asp-route-id="item.PersonelId">Edit</a> |
                <a asp-action="PersonelDetails" asp-route-id="item.PersonelId">Details</a> |
                <a asp-action="PersonelDelete" asp-route-id="item.PersonelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
-->

<div class="container mt-5">
    <h2 class="text-center mb-4">Personel Listesi</h2>

   

    <div class="card shadow-lg">
        <div class="card-body">
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.PersonelAd)</th>
                        <th>@Html.DisplayNameFor(model => model.PersonelSoyad)</th>
                        <th>@Html.DisplayNameFor(model => model.Islem)</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.PersonelAd)</td>
                            <td>@Html.DisplayFor(modelItem => item.PersonelSoyad)</td>
                            <td>@Html.DisplayFor(modelItem => item.Islem.IslemAd)</td>
                            <td>
                                <a asp-action="PersonelEdit" asp-route-id="@item.PersonelId" class="btn btn-secondary btn-sm">Düzenle</a>
                                <a asp-action="PersonelDetails" asp-route-id="@item.PersonelId" class="btn btn-info btn-sm">Detaylar</a>
                                <a asp-action="PersonelDelete" asp-route-id="@item.PersonelId" class="btn btn-danger btn-sm">Sil</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            <div class="mb-3 text-right">
                <a asp-action="PersonelCreate" class="btn btn-outline-success btn-lg">Yeni Personel Ekle</a>
            </div>
        </div>
    </div>
</div>